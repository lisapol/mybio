## PIP
`python --version`

`pip --version`

`pip install --upgrade pip`

`pip list`

`pip install -r requirements.txt`

requests installed along with urllib3, certifi, charset-normalizer, idna

## Biomarkers
- chemical;
- protein;
- genetic;
- karyotype (?).